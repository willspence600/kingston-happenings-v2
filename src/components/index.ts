/**
 * Components barrel export
 * 
 * This file provides convenient imports for all components.
 * Components are organized into subdirectories:
 * 
 * - ui/      - Generic UI components (Modal, Toast, DatePicker)
 * - events/  - Event-related components (EventCard, CategoryFilter)
 * - venues/  - Venue-related components (VenueSelector)
 * - layout/  - Layout components (Navigation, Footer)
 */

// UI Components
export { Modal, Toast, DatePicker } from './ui';

// Event Components
export { EventCard, CategoryFilter } from './events';

// Venue Components
export { VenueSelector } from './venues';

// Layout Components
export { Navigation, Footer } from './layout';

